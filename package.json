{
  "name": "@modulora/spec",
  "version": "0.0.0",
  "private": true,
  "description": "Open specifications for Modulora manifests, provenance, trust evidence, and API contracts.",
  "license": "Apache-2.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Modulora/spec.git"
  },
  "scripts": {
    "test": "node --test 'test/**/*.test.mjs'",
    "validate": "node scripts/validate-fixtures.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
